# Nginx Map variables - generated by agent from nginx_config.maps
# Placeholder: will be replaced by agent on config pull

# Default maps (overridden by UHDadmin config)
map $request_method $deny_delete {
    default 0;
    DELETE  1;
}

map $request_method $deny_post {
    default 0;
    POST    1;
}
